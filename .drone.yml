pipeline:
    build:
        image: alpine
        commands:
          - apk update
          - apk add npm curl jq
          - npm config set unsafe-perm true
          - npm install -g markdownlint-cli
          - markdownlint README.md
